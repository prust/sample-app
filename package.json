{
  "name": "sample-app",
  "version": "0.0.1",
  "description": "Small app for learning TypeScript",
  "scripts": {
    "build": "tsc --build",
    "start": "npm run build && node server/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/node18": "^18.2.1",
    "@tsconfig/recommended": "^1.0.2",
    "@types/express": "^4.17.17",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
